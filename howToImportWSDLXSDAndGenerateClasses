Guide to Importing WSDL/XSD Files and Generating Classes
========================================================

Version
-------

Date: February 25\
Performed by: Steffen Lomholt Hansen\
Version: 1.0\
March 11, 2025\
Christoffer Dahl Rexen\
Version: 1.1

Java
----

This section describes how to import WSDL/XSD files and generate classes in Java.

### Placement of WSDL and XSD Files

The WSDL files for YdelsesIndeks should be placed in an appropriate subfolder under src/main/resources, e.g., wsdl/YdelsesIndeks/wsdl.

When opening the WSDL files in a text editor, you can see the schemaLocation for the included XSD files, for example:

"../xsd/6.0.STS-0/YdelseIndeksOperationer.xsd"

In this case, the schemaLocation means that YdelseIndeksOperationer.xsd should be placed in the subfolder wsdl/YdelsesIndeks/xsd/6.0.STS-0 under src/main/resources.

### Configuration of pom.xml

In the code examples, the cxf-codegen-plugin version 3.3.6 is used to auto-generate the service's incoming POJOs. This is shown in the following excerpt from pom.xml, where cxf.version is 3.3.6:\
<plugin>\
    <groupId>org.apache.cxf</groupId>\
    <artifactId>cxf-codegen-plugin</artifactId>\
    <version>${cxf.version}</version>\
    <executions>\
        <execution>\
            <id>generate-sources</id>\
            <phase>generate-sources</phase>\
            <configuration>\
                <sourceRoot>${project.build.directory}/generated-sources/cxf</sourceRoot>\
                <wsdlOptions>\
                    <wsdlOption>\
                        <wsdl>${resources}/${wsdl.YdelseIndeks}</wsdl>\
                        <wsdlLocation>classpath:${wsdl.YdelseIndeks}</wsdlLocation>\
                    </wsdlOption>\
                </wsdlOptions>\
            </configuration>\
            <goals>\
                <goal>wsdl2java</goal>\
            </goals>\
        </execution>\
    </executions>\
</plugin>

### Generating Classes

The service's incoming classes are generated by running the following command in Maven:

mvn clean compile

.NET
----

This section describes how to import WSDL/XSD files and generate classes in .NET 4.8 and .NET 6.

### Placement of WSDL and XSD Files

The WSDL files for YdelsesIndeks should be placed in an appropriate subfolder of the .NET project, e.g., wsdl/YdelsesIndeks/wsdl.

### Configuration of Windows Batch File\
svcutil.exe ^\
 wsdl\YdelseIndeks.wsdl ^\
 wsdl\xsd\6.0.STS-0\*.xsd ^\
 /noLogo /n:"*,Kombit.InfrastructureSamples.YdelseIndeksService" /d:..\..\Kombit.InfrastructureSamples\YdelsesIndeks\ /o:YdelseIndeksService.cs

### Generating Classes

The service's incoming classes are generated by running the batch file run.bat in the Developer Command Prompt or Developer PowerShell from Visual Studio.
